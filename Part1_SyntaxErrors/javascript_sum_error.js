// JavaScript : Calculate the sum of an array with intentional errors
function calculateSum(arr) {
  let total = o; // <-- ERROR: letter 'o' instead of number 0
  for (let num of arr) {
    total += num;
  }
  return total;
}

let numbers = [1, 2, 3, 4, 5];
let result = calculate Sum (numbers); // <-- ERROR: space splits identifier
console.log("Sum in JavaScript:", result);
